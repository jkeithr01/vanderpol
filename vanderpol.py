# vanderpol.py
# A program to generate the output of a Van der Pol oscillator
# INPUT: model parameters gathered from command prompt input
#   note: a future version can have a GUI, like a web form
# OUTPUT: a 2-d array of data points, [time(i), output(i)]
#   output will be written to CSV at first, 
#   but can later be diplayed in the GUI (web form response)

# gather input parameters

# run model and generate output array

# write output

